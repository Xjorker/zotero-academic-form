# 2026-04-02 工作日志

## Phase 2 测试运行 + Bug 修复

### 完成内容
- 运行 Phase 2 测试脚本 `test_phase2.py`（8 项测试）
- 修复 3 个 Bug（详见 MEMORY.md）
- 最终 8/8 全部通过

### 修复的 Bug
1. **Chroma n_results=0 崩溃** — `kb/vector_store.py` 中 collection 为空时 `min(top_k, 0)` 返回 0 导致报错。修复为提前 return []
2. **@tool 函数名冲突** — `agent/tools/query_kb.py` 中 `@tool def get_kb_schema` 覆盖了同名的导入函数，导致递归调用。修复为使用别名导入
3. **validate_sql_safety 误判** — `kb/database.py` 中 `SELECT 1` 不含表名被误判。修复为放行不含 FROM/JOIN 的 SELECT

### 测试架构改进
- `test_phase2.py` 改为子进程模式运行（避免 SQLite WAL 文件锁定）
- 每次测试前自动清理 `data/` 目录
- 实际测试逻辑在 `_test_phase2_inner.py`

## Phase 3 前端简化（执行完成）

### 修改文件清单

| 文件 | 操作 | 行数变化 |
|------|------|---------|
| `academicForm.ts` | 重写 | 1195→~380 行（-68%） |
| `agentClient.ts` | 更新 | 205→220 行（新增 llm_config 传递） |
| `llmProcessor.ts` | 标记 @deprecated | 676 行，添加废弃标记 |
| `database.ts` | 清理空壳函数 | 289→261 行（-7个 throw not implemented） |
| `authorProfile.ts` | 删除硬编码 API Key | 1037→~1037 行（改用 getLLMConfig()） |
| `examples.ts` | 删除无用类 | 867→~115 行（-87%） |
| `hooks.ts` | 清理无用调用 | 209→~190 行 |
| `preferenceScript.ts` | 添加知识库面板 | 4395→~4580 行（+185行，知识库初始化UI） |

### 核心变更
1. **academicForm.ts**：前端不再直接调用 LLM（5次→0次），改为调用 `callAgentFill()` 一次完成
2. **agentClient.ts**：`callAgentFill()` 自动从偏好设置读取 LLM 配置，传递给后端
3. **authorProfile.ts**：删除硬编码 `DEEPSEEK_API_KEY`，`callDeepseekChat()` → `callLLMChat()`
4. **examples.ts**：删除 `PromptExampleFactory`（590行）和 `HelperExampleFactory`（270行）
5. **hooks.ts**：删除 `onDialogEvents` 函数，移除 `UIExampleFactory.registerExtraColumn` 等
6. **preferenceScript.ts**：在偏好设置面板添加「知识库初始化」区域（上传CV/填URL → 一键导入）

### 编译验证
- `agentClient.ts`、`authorProfile.ts` 的新增类型错误已修复
- 剩余错误均为已有模式（Zotero XPCOM 类型声明不完整），未引入新错误
